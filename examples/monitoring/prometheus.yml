global:
  scrape_interval: 15s

rule_files:
  # Generate this file with:
  #   gpu-holder alerts > gpu-holder-alerts.yml
  - gpu-holder-alerts.yml

scrape_configs:
  - job_name: node
    static_configs:
      - targets:
          - localhost:9100
